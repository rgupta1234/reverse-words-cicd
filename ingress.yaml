apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: reverse-words
  annotations:
    kubernetes.io/ingress.class: nginx
    argocd.argoproj.io/compare-options: IgnoreExtraneous
spec:
  rules:
  - host: reversewords-dev.apps.cluster-f1b7.f1b7.example.opentlc.com
    http:
      paths:
      - backend:
          serviceName: reverse-words
          servicePort: http
